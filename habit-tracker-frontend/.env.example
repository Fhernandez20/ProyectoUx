# Copia este archivo como ".env.local" y ajusta el valor si hace falta.
# El archivo ".env.local" NO se sube a GitHub.

# URL del backend (opcional). Si se omite, se usa http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000
